- hosts: localhost
  become: true

  tasks:
   - name: Install NGINX
     dnf:
       name: nginx
       state: present
   - name: Start Nginx service
     service:
       name: nginx
       state: started
       enabled: yes
